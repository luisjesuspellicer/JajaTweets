'use strict';

describe('myApp.singin module', function() {

  beforeEach(module('myApp.singin'));

  describe('singin controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});