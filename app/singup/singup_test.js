'use strict';

describe('myApp.singup module', function() {

  beforeEach(module('myApp.singup'));

  describe('singup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var singupCtrl = $controller('singupCtrl');
      expect(singupCtrl).toBeDefined();
    }));

  });
});