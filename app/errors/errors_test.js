'use strict';

describe('myApp.errors module', function() {

  beforeEach(module('myApp.errors'));

  describe('errors controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var errorsCtrl = $controller('errorsCtrl');
      expect(errorsCtrl).toBeDefined();
    }));

  });
});